{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:seed": "tsx server/database/seed.ts",
    "db:reset": "rm -f server/database/db.sqlite && rm -rf server/database/migrations && pnpm db:generate && drizzle-kit migrate && pnpm db:seed",
    "test": "vitest"
  },
  "dependencies": {
    "@nuxt/icon": "^2.0.0",
    "@types/bcrypt": "^6.0.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/jsonwebtoken": "^9.0.10",
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^12.4.1",
    "drizzle-orm": "^0.44.5",
    "jsonwebtoken": "^9.0.2",
    "nuxt": "^4.1.2",
    "vue": "^3.5.21",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@nuxt/test-utils": "^3.19.2",
    "@vitest/ui": "^3.2.4",
    "drizzle-kit": "^0.31.4",
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  }
}
